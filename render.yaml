services:
  - type: web
    name: fitness-controller-pro

    runtime: python

    plan: free

    autoDeploy: true

    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: |
      python run.py

    healthCheckPath: /health
